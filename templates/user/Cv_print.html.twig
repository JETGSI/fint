<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
    <title>FINT | Candidatures réçus | {{ student.firstName ~ " " ~  student.lastName }}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset("build/images/X-_1_.png") }}">

</head>
<style>

    .edu .body h5, .donnee_personelle h1, hr, .Pro .body h5, .Asso .body h5{
        color:#e30719
    }
    hr{
        width:80%;
        border-top: 3px solid #e30719;
    }
    img{
        width: 200px;
        height: auto;
        margin: 10px;
    }

    .container{
        width: 1200px;
        padding: 20px;
        margin-top: 10px;
        border: 3px solid #000000;
        border-radius: 25px;
    }
</style>
<body   class="container">
    <div>
        <div class="d-flex justify-content-around">
            <img src="{{ asset(student.img) }}"  alt="image de cv">
            <div class="donnee_personelle">
                <h1>{{ student.firstName ~ " " ~  student.lastName }}</h1>
                <h3>{{ student.address }}</h3>
                <div> <p>{{ user.email }}</p></div>
                <div> <p> Telephone</p></div>
                <div> <p>{{ student.linkedinLink }}</p></div>
            </div>
        </div>
        <div>
            <div class="edu">
                <h2>Experience educative</h2>
                {% for i in edu %}
                <div class="body">
                    <div class="d-flex justify-content-between">
                    <h5>{{ i.University }}</h5>
{#                    <h6>{{ i.startDate}}</h6>#}
                    </div>
                    <p>{{ i.description }}</p>
                </div>
                {% endfor %}
            </div>
            <hr>
            <div class="Pro">
                <h2>Experience Professionelle</h2>
                {% for i in pro %}
                <div class="body">
                    <div class="d-flex justify-content-between">
                        <h5>{{ i.entreprise }}</h5>
{#                        <h6>{{ i.period[0]}}</h6>#}
                    </div>
                    <p>{{ i.type }} {{ i.poste }}</p>
                    <p>{{ i.description }}</p>
                </div>
                {% endfor %}
            <hr>
            <div class="Asso">
                <h2>Experience Associative</h2>
                {% for i in asso %}
                <div class="body">
                    <div class="d-flex justify-content-between">
                        <h5>{{ i.organization }}</h5>
{#                        <h6>{{ i.period[0]}}</h6>#}
                    </div>
                    <p>{{ i.description }}</p>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>
</html>